import React from 'react';

const Navbar: React.FC = () => (
  <nav
    style={{
      background: '#222',
      color: '#fff',
      padding: '1rem',
      textAlign: 'center',
      fontWeight: 'bold',
    }}
  >
    SNF
  </nav>
);

export default Navbar;
